import { blob, index, integer, sqliteTable, text } from "drizzle-orm/sqlite-core";
export const leaderboard = sqliteTable("leaderboard", { id: text("id").primaryKey(), leftName: text("left_name").notNull(), rightName: text("right_name").notNull(), score: integer("score").notNull(), leftImage: blob("left_image", { mode: "buffer" }).notNull(), rightImage: blob("right_image", { mode: "buffer" }).notNull(), imageType: text("image_type").notNull(), createdAt: integer("created_at").notNull() }, table => [index("idx_leaderboard_rank").on(table.score, table.createdAt)]);
